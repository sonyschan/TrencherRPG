# idleTrencher PWA 轉換企劃書

## 目錄

1. [執行摘要](#執行摘要)
2. [技術需求分析](#技術需求分析)
3. [Privy 與 PWA 整合](#privy-與-pwa-整合)
4. [iOS vs Android 平台差異](#ios-vs-android-平台差異)
5. [手機 UI/UX 改版規劃](#手機-uiux-改版規劃)
6. [工作項目與估算](#工作項目與估算)
7. [建議開發順序](#建議開發順序)

---

## 執行摘要

### 專案概述
將 idleTrencher 網頁應用轉換為 Progressive Web App (PWA)，讓使用者可在手機上以類原生 App 方式使用。

### 可行性評估
**結論：可行，但需要注意以下關鍵點**

| 面向 | 評估 | 說明 |
|------|------|------|
| Privy 整合 | ✅ 官方支援 | Privy 提供官方 PWA 模板，推薦使用 Embedded Wallet |
| Three.js 3D | ⚠️ 需優化 | 手機 GPU 較弱，需降低繪圖品質 |
| iOS 限制 | ⚠️ 需處理 | Safari PWA 有 50MB 快取限制、無推播通知（iOS 16.4+ 可） |
| 錢包連接 | ⚠️ 建議調整 | 外部錢包需 App 切換，建議優先使用 Embedded Wallet |

### 預估工時

| 階段 | 工作量 |
|------|--------|
| Phase 1: 基礎 PWA 設置 | 2-3 天 |
| Phase 2: 手機 UI 適配 | 5-7 天 |
| Phase 3: 3D 效能優化 | 3-5 天 |
| Phase 4: 測試與修正 | 3-5 天 |
| **總計** | **13-20 天** |

---

## 技術需求分析

### 1. PWA 核心檔案

需要新增以下檔案：

```
frontend/
├── public/
│   ├── manifest.json          # PWA 清單（新增）
│   ├── sw.js                  # Service Worker（新增）
│   ├── icons/                 # App 圖示（新增）
│   │   ├── icon-72x72.png
│   │   ├── icon-96x96.png
│   │   ├── icon-128x128.png
│   │   ├── icon-144x144.png
│   │   ├── icon-152x152.png
│   │   ├── icon-192x192.png
│   │   ├── icon-384x384.png
│   │   └── icon-512x512.png
│   └── apple-touch-icon.png   # iOS 專用圖示
├── index.html                 # 修改：加入 PWA meta tags
└── vite.config.js             # 修改：PWA plugin 設定
```

### 2. manifest.json 範例

```json
{
  "name": "idleTrencher",
  "short_name": "idleTrencher",
  "description": "Turn your Solana token holdings into RPG partners",
  "start_url": "/",
  "display": "standalone",
  "orientation": "portrait",
  "background_color": "#0a0a0f",
  "theme_color": "#6366f1",
  "icons": [
    {
      "src": "/icons/icon-192x192.png",
      "sizes": "192x192",
      "type": "image/png"
    },
    {
      "src": "/icons/icon-512x512.png",
      "sizes": "512x512",
      "type": "image/png"
    }
  ]
}
```

### 3. index.html 需新增的 Meta Tags

```html
<!-- PWA Meta Tags -->
<meta name="theme-color" content="#6366f1">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
<meta name="apple-mobile-web-app-title" content="idleTrencher">
<link rel="apple-touch-icon" href="/apple-touch-icon.png">
<link rel="manifest" href="/manifest.json">

<!-- Prevent zoom on input focus (iOS) -->
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
```

### 4. Vite PWA Plugin 設定

```bash
npm install vite-plugin-pwa -D
```

```javascript
// vite.config.js
import { VitePWA } from 'vite-plugin-pwa';

export default defineConfig({
  plugins: [
    react(),
    VitePWA({
      registerType: 'autoUpdate',
      workbox: {
        globPatterns: ['**/*.{js,css,html,ico,png,svg,glb,gltf}'],
        maximumFileSizeToCacheInBytes: 10 * 1024 * 1024, // 10MB for 3D assets
      },
      manifest: {
        // ... manifest 內容
      }
    })
  ]
});
```

---

## Privy 與 PWA 整合

### 官方支援情況

Privy 官方對 PWA 有良好支援：
- 提供 `create-privy-pwa` 官方模板
- 官方 Blog 文章說明 PWA + 錢包整合最佳實踐
- 推薦使用 **Embedded Wallet** 作為 PWA 首選方案

### 錢包連接策略

| 錢包類型 | PWA 支援度 | 說明 |
|----------|------------|------|
| **Embedded Wallet** | ✅ 最佳 | 完全嵌入在 App 內，無需切換應用程式 |
| WalletConnect | ⚠️ 可用 | 需要使用者切換到錢包 App 簽名後返回 |
| Browser Extension | ❌ 不適用 | PWA 無法存取瀏覽器擴充功能 |

### 建議的 Privy 設定

```javascript
// PrivyProvider config for PWA
<PrivyProvider
  appId={PRIVY_APP_ID}
  config={{
    loginMethods: ['email', 'sms', 'google', 'apple'],
    embeddedWallets: {
      createOnLogin: 'users-without-wallets',
      noPromptOnSignature: true, // 減少簽名彈窗
    },
    // PWA 優先使用 embedded wallet
    appearance: {
      walletList: ['detected_wallets', 'embedded'],
    }
  }}
>
```

### PWA 特定處理

1. **Deep Link 處理**：當使用者從外部錢包返回時需要正確處理
2. **Session 持久化**：PWA 關閉後重開需保持登入狀態
3. **Offline 提示**：離線時顯示適當提示，而非錯誤

---

## iOS vs Android 平台差異

### iOS Safari PWA 限制

| 限制 | 影響 | 解決方案 |
|------|------|----------|
| **50MB 快取限制** | 3D 模型可能超限 | 壓縮 GLB、動態載入、實作 LRU 快取 |
| **7 天未使用清除快取** | 使用者資料遺失 | 引導使用者常開 App、重要資料存後端 |
| **無 Service Worker Background Sync** | 離線操作無法同步 | 改用線上即時同步 |
| **推播需 iOS 16.4+** | 舊裝置無推播 | 提供替代通知方式 |
| **無法在 Home Screen 前隱藏 URL Bar** | 視覺差異 | 使用 `display: standalone` |
| **手動安裝流程** | 使用者不熟悉 | 提供安裝引導 UI |

### Android PWA 優勢

| 功能 | iOS | Android |
|------|-----|---------|
| 自動安裝提示 | ❌ | ✅ |
| 推播通知 | iOS 16.4+ | ✅ |
| Background Sync | ❌ | ✅ |
| 快取限制 | 50MB | 較寬鬆 |
| Play Store 上架 | ❌ | ✅ (TWA) |

### 建議策略

**不需要分開開發 iOS/Android 版本**

理由：
1. PWA 本質是同一份程式碼
2. 差異主要在於系統限制，而非功能實作
3. 使用 feature detection 處理差異即可

```javascript
// 範例：偵測 iOS PWA
const isIOSPWA = () => {
  return (
    window.navigator.standalone === true ||
    window.matchMedia('(display-mode: standalone)').matches
  ) && /iPhone|iPad|iPod/.test(navigator.userAgent);
};

// 根據平台調整快取策略
if (isIOSPWA()) {
  // iOS: 更積極的資源壓縮和懶載入
}
```

---

## 手機 UI/UX 改版規劃

### 現有桌面版 Layout 分析

```
┌─────────────────────────────────────────────────────────┐
│  Header (60px)                                          │
│  [Logo] [Stats: Value/Change] [Refresh] [Wallet] [⚙️]   │
├────────────────────────────────────┬────────────────────┤
│                                    │                    │
│        3D Farm Scene               │   Partner List     │
│        (主要內容區)                 │   (280px 寬)       │
│                                    │                    │
│                                    │   - Partner 1      │
│                                    │   - Partner 2      │
│                                    │   - Partner 3      │
│                                    │   - ...            │
│                                    │                    │
├────────────────────────────────────┴────────────────────┤
│  Feature Menu (底部右側)                                 │
│  [🏠 Home] [🚪 Explore] [👕 Skin] [🏆 Leaderboard]       │
└─────────────────────────────────────────────────────────┘
```

### 手機版 Layout 規劃

```
Portrait Mode (直向 - 主要模式)
┌─────────────────────────┐
│  Header (簡化版, 48px)   │
│  [Logo] [Wallet] [⚙️]    │
├─────────────────────────┤
│                         │
│     3D Farm Scene       │
│     (全螢幕主視覺)       │
│                         │
│    [點擊角色開對話框]    │
│                         │
├─────────────────────────┤
│  Stats Bar (40px)       │
│  💰 $1,234  📈 +5.2%    │
├─────────────────────────┤
│  Partner Carousel       │
│  [◀] [P1] [P2] [P3] [▶] │
│  (水平滑動選擇 Partner) │
├─────────────────────────┤
│  Bottom Nav (56px)      │
│  [🏠] [🔍] [👕] [🏆]     │
└─────────────────────────┘
```

### 各元件改版細節

#### 1. Header 簡化

**隱藏/移除：**
- 總價值與變化統計（移到專屬 Stats Bar）
- Refresh 按鈕文字（改為只有圖示）
- Last Updated 時間（改為圖示提示）

**保留/強化：**
- Logo（可點擊回首頁）
- 錢包連接狀態（縮短地址顯示）
- 設定齒輪

**程式碼改動：**
```css
/* Header.css 新增 */
@media (max-width: 768px) {
  .header {
    padding: 8px 12px;
    height: 48px;
  }

  .header-center {
    display: none; /* 隱藏中間統計區 */
  }

  .wallet-address {
    max-width: 80px;
    font-size: 11px;
  }

  .btn-refresh .refresh-text {
    display: none; /* 只顯示圖示 */
  }

  .last-updated {
    display: none;
  }
}
```

#### 2. Partner List → Partner Carousel

**改版重點：**
- 從側邊欄改為底部水平輪播
- 支援左右滑動手勢
- 當前選中的 Partner 在 3D 場景中高亮

**新增元件：** `PartnerCarousel.jsx`

```jsx
// 簡化示意
function PartnerCarousel({ partners, selectedIndex, onSelect }) {
  return (
    <div className="partner-carousel">
      <div className="carousel-track" style={{ transform: `translateX(-${selectedIndex * 100}px)` }}>
        {partners.map((partner, i) => (
          <div
            key={partner.id}
            className={`carousel-item ${i === selectedIndex ? 'active' : ''}`}
            onClick={() => onSelect(i)}
          >
            <img src={partner.logo} alt={partner.symbol} />
            <span className="partner-level">Lv{partner.level}</span>
          </div>
        ))}
      </div>
    </div>
  );
}
```

#### 3. 3D Scene 互動優化

**改動：**
- 觸控手勢：雙指縮放、單指旋轉
- 點擊角色直接開啟對話框（取代桌面版的滑鼠懸停）
- 簡化攝影機控制（移除 WASD 鍵盤操作）

**程式碼改動 (FarmScene.js)：**
```javascript
// 偵測觸控裝置
this.isTouchDevice = 'ontouchstart' in window;

if (this.isTouchDevice) {
  // 觸控優化
  this.controls.enablePan = false;        // 禁用平移
  this.controls.rotateSpeed = 0.5;        // 降低旋轉速度
  this.controls.enableDamping = true;     // 啟用阻尼
  this.controls.dampingFactor = 0.1;
}
```

#### 4. Stats Bar 新元件

**位置：** 3D 場景下方、Partner Carousel 上方

**顯示內容：**
- 總持倉價值
- 24 小時變化百分比
- 上次更新時間（簡化為「剛剛」/「5 分鐘前」）

**新增檔案：** `StatsBar.jsx`, `StatsBar.css`

#### 5. Bottom Navigation

**替代：** 桌面版右下角的 Feature Menu

**項目：**
| 圖示 | 功能 | 說明 |
|------|------|------|
| 🏠 | Home | 回到自己的農場 |
| 🔍 | Explore | 探索他人錢包 |
| 👕 | Skin | 更換角色外觀 (Coming Soon) |
| 🏆 | Rank | 排行榜 (Coming Soon) |

**新增檔案：** `BottomNav.jsx`, `BottomNav.css`

#### 6. Token Detail Modal 適配

**現有問題：**
- 500px max-width 在小螢幕可能過寬
- 對話框文字可能過多需要捲動

**改動：**
```css
@media (max-width: 768px) {
  .modal-container {
    width: 95%;
    max-width: none;
    max-height: 80vh;
    overflow-y: auto;
  }

  .dialog-text {
    font-size: 14px;
    min-height: auto;
    max-height: 150px;
    overflow-y: auto;
  }

  .token-avatar-large {
    width: 80px;
    height: 80px;
  }
}
```

#### 7. 安裝引導 UI（iOS 專用）

**觸發條件：** 首次訪問且為 iOS Safari

**引導步驟：**
1. 點擊分享按鈕 📤
2. 選擇「加入主畫面」
3. 點擊「新增」

**新增檔案：** `InstallPrompt.jsx`, `InstallPrompt.css`

---

## 工作項目與估算

### Phase 1: 基礎 PWA 設置 (2-3 天)

| 項目 | 說明 | 估計時間 |
|------|------|----------|
| 1.1 安裝 vite-plugin-pwa | npm install 並設定 | 0.5 天 |
| 1.2 建立 manifest.json | App 名稱、圖示、顏色 | 0.5 天 |
| 1.3 設計並切割 App 圖示 | 8 種尺寸 + Apple Touch Icon | 1 天 |
| 1.4 修改 index.html | 加入 PWA meta tags | 0.5 天 |
| 1.5 Service Worker 基礎設定 | 快取策略配置 | 0.5 天 |

### Phase 2: 手機 UI 適配 (5-7 天)

| 項目 | 說明 | 估計時間 |
|------|------|----------|
| 2.1 Header 響應式改版 | 簡化佈局、隱藏元素 | 1 天 |
| 2.2 新增 StatsBar 元件 | 持倉統計橫條 | 0.5 天 |
| 2.3 新增 PartnerCarousel | 底部水平輪播 | 1.5 天 |
| 2.4 新增 BottomNav | 底部導航列 | 1 天 |
| 2.5 TokenDetailModal 適配 | 小螢幕對話框調整 | 0.5 天 |
| 2.6 ExploreDialog 適配 | 探索功能手機版 | 0.5 天 |
| 2.7 SettingsMenu 適配 | 設定選單全螢幕化 | 0.5 天 |
| 2.8 InstallPrompt (iOS) | 安裝引導 UI | 1 天 |

### Phase 3: 3D 效能優化 (3-5 天)

| 項目 | 說明 | 估計時間 |
|------|------|----------|
| 3.1 GLB 模型壓縮 | 使用 gltf-transform 壓縮 | 1 天 |
| 3.2 觸控控制優化 | 手勢旋轉、縮放調整 | 1 天 |
| 3.3 動態品質調整 | 根據裝置效能降低繪圖品質 | 1 天 |
| 3.4 懶載入策略 | 只載入可視範圍內的角色模型 | 1-2 天 |

### Phase 4: 測試與修正 (3-5 天)

| 項目 | 說明 | 估計時間 |
|------|------|----------|
| 4.1 iOS Safari 測試 | iPhone 各尺寸測試 | 1 天 |
| 4.2 Android Chrome 測試 | 各廠牌裝置測試 | 1 天 |
| 4.3 Privy 錢包流程測試 | 登入、簽名、斷線重連 | 1 天 |
| 4.4 Bug 修正 | 修正測試發現的問題 | 1-2 天 |

---

## 建議開發順序

### 第一優先 (MVP)

1. **PWA 基礎設置** - 讓 App 可安裝到手機桌面
2. **Header 簡化** - 最基本的 UI 適配
3. **3D 觸控優化** - 確保核心互動可用
4. **BottomNav** - 基本導航功能

**MVP 預估：5-7 天**

### 第二優先 (完整體驗)

5. **PartnerCarousel** - 優化 Partner 選擇體驗
6. **StatsBar** - 持倉資訊一目瞭然
7. **TokenDetailModal 適配** - 完整對話功能
8. **InstallPrompt** - iOS 安裝引導

**第二階段預估：4-6 天**

### 第三優先 (效能優化)

9. **GLB 壓縮** - 減少載入時間
10. **動態品質調整** - 低階裝置流暢度
11. **完整測試** - 跨裝置相容性

**第三階段預估：4-7 天**

---

## 風險與應對

| 風險 | 可能影響 | 應對策略 |
|------|----------|----------|
| iOS 快取清除 | 使用者資料遺失 | 關鍵資料存後端 DB |
| 3D 效能不足 | 低階手機卡頓 | 提供 2D 降級模式 |
| 外部錢包切換流程差 | 使用者流失 | 強推 Embedded Wallet |
| 安裝流程不直覺 | 轉換率低 | 精心設計引導 UI |

---

## 結論

PWA 轉換是可行的，且 Privy 官方已有良好支援。建議優先開發 MVP 版本（約 1 週），驗證核心使用場景後再投入完整開發。

**關鍵成功因素：**
1. 優先使用 Embedded Wallet 避免 App 切換問題
2. 3D 場景必須針對手機效能優化
3. iOS 安裝引導要足夠清晰

**不建議分開開發 iOS/Android 版本**，使用 feature detection 處理平台差異即可。
